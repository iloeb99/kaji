func int main() {
    str s;
    str t;

    s = "Hello!";
    t = "Goodbye!";
    
	printStr(s);
	printStr(t);

    freeStr(s);
    s = t;
    t = ":)";
	
	printStr(s);
	printStr(t);

    freeStr(t);

    print(1);

    return 0;
}

